name: scrolling_subtitles
description: Lets you view subtitles of a video in scrolling format.
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.19.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  bordered_text: ^2.0.0
  cupertino_icons: ^1.0.2
  google_fonts: ^4.0.3
  subtitle: ^0.1.0-beta.3
  dart_vlc: ^0.4.0
  blur: ^3.1.0
  file_picker: ^5.2.5
  # auto_size_text: ^3.0.0
  provider: ^6.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
